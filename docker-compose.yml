version: '3.8'

services:
  backend:
    build: ./Backend

    ports: 
    - "5000:5000"

    environment:
    - PORT=5000
    - REDIS_URL=redis://redis:6379

    depends_on:
    - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

